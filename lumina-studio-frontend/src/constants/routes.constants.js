/**
 * @file routes.constants.js
 * @description Centralized route path constants. Use these in AppRouter and
 * all navigation calls — never hard-code path strings.
 */

/** @type {Object.<string, string>} */
export const ROUTES = {
    HOME: '/',
    LOGIN: '/login',
    STORE: '/store',
    NOT_FOUND: '*',
};
