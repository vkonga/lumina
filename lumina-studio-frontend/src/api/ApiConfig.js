/**
 * @file ApiConfig.js
 * @description Re-exports the resolved environment config for any module that
 * needs API transport settings without importing from config/env directly.
 */
export { default } from '../config/env';
